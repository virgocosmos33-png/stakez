/** Ways-link skim. Two wipes, two whip cracks, then the rim sits. */
export const SHINE_SWEEP_MS = 280;
export const SHINE_SWEEP_COUNT = 2;
/** Sit after the last visual lash so money is not glued to the whip. */
export const SHINE_AFTER_MS = 180;
export const SHINE_CAP_MS = SHINE_SWEEP_MS * SHINE_SWEEP_COUNT + SHINE_AFTER_MS;
/** Small-win sting waits so it reads as overlay, not a third board hit. */
export const SHINE_MONEY_STING_MS = 220;
